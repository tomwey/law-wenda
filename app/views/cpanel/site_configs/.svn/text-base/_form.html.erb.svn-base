<%= simple_form_for [:cpanel, @site_config] do |f| %>
	<%= f.input :value, as: :text, :input_html => { :class => "span6", :style => "height: 300px"} %>
	<%= f.submit "保存", :class => "btn btn-primary", :disable_with => t("common.submitting") %>
<% end %>